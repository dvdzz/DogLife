<% @dogs.each do |dog| %>
  <div class="booyah-box col-10 offset-1">
    <h1><%= dog.name %></h1>
    <i><%= dog.caption %></i><br /><br />
  </div>
<% end %>




<%= will_paginate @page, previous_label: h("<"), next_label: h(">") %>